# API Reference

## Quickstart
To get your development environment running immediately, use the following commands from the project root:

```bash
# 1. Install dependencies
pip install -r requirements.txt

# 2. Build the documentation
make build-docs

# 3. Start the local development server
mkdocs serve
